  <div class="home_section">
    <div class="home_content">
        <div class="profil_perso">
          <div>
            <% if user_signed_in? %>
              <%= button_to "Log out", destroy_user_session_path, method: :delete, class: 'sign_out' %>
            <% else %>
              <%= button_to "Sign in", new_user_session_path, class: 'sign_in' %>
            <% end %>
          </div>
          <div class="notification">
            <i class='bx bxs-message-alt'></i>
            <i class='bx bx-message-alt'></i>
          </div>
          <%= image_tag 'pp.jpg', class: 'pp' %>
          <% if user_signed_in? %>
          <h3><%= current_user.username %></h3>
          <% end %>
      </div>
    </div>
  </div>
  <div class="asking_modifications">
    <div class="asking_profil">
      <div class="profil">
      <%= image_tag 'nathan.jpg', class: 'pp' %>
      <div class="nom_et_statut">
        <h3>Nathian Berthier</h3>
      </div>
      </div>
      <i class='bx bx-transfer'></i>
      <div class="profil">
      <%= image_tag 'bruno.jpg', class: 'pp' %>
      <div class="nom_et_statut">
        <h3>Bruno Latourelle</h3>
      </div>
      </div>
        <p>
        Echange pour le créneau de Mardi 15 Septembre
        </p>
      <div class="bouton_validation">
        <h4>Valider la demande</h4>
        <h4>Refuser la demande</h4>
      </div>
    </div>
  </div>
